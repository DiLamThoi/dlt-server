const name = 'Employer';
const definition = {
    email: String,
    name: String,
    logo: String,
    address: String,
    website: String,
    description: String,
    // authentication
    userName: String,
    password: String,
};

module.exports = {
    name,
    definition,
};
