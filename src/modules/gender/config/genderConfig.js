const name = 'Gender';
const definition = {
    name: String, // Tên giới tính (Vietnamese)
    value: Number, // 1: Nam, -1: Nữ, 0: Khác (Use for language processing)
};

module.exports = {
    name,
    definition,
};